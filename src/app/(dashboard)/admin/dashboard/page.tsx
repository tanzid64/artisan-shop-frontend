import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin Dashboard | Artisan Shop",
  description: "Admin Dashboard",
};
export default function AdminDashboardPage() {
  return <div>Admin Dashboard</div>;
}
